This folder is to be used to re-write codes for all ML algorithms.

I'm not supposed to use the library but instead code the algorithm raw.

This should allow me to understand the working of the algorithm and hopefully also expose some of the math that is going on behind the algorithm.